name: alfager
description: "Al-Fajr Wake-up and Prayer Follow-up System - Production Grade"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.20.3
  flutter_local_notifications: ^17.1.1
  adhan: ^2.0.0+1
  geolocator: ^11.0.0
  flutter_compass: ^0.7.0
  shared_preferences: ^2.2.3
  http: ^1.2.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  audioplayers: ^5.2.1
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
